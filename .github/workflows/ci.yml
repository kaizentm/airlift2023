name: Deploy infrastructure

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      environment_name:
        description: 'Environment name'
        required: true
        default: 'prod'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Credential scanning
        run: |
          #!/bin/bash
          if grep -ri "password: '" ./bicep ; then
            echo "Found passwords in files 😦"

            exit 1;
          else
            echo "No passwords found in files 👏"
          fi

      - name: Azure Login
        uses: Azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy infrastructure
        env:
          sqlServerAdministratorPassword: ${{ secrets.sqlServerAdministratorPassword }}
          environmentName: ${{ github.event.inputs.environment_name }}
        run: |
          AZ_SUBSCRIPTION_ID=$(az account show --query id --output tsv)
          AZ_TENANT_ID=$(az account show --query tenantId --output tsv)
          AZ_APPLICATION_ID="${{ secrets.AZURE_CREDENTIALS }} | jq -r .clientId"
          ./deploy.sh
